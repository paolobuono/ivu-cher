<UserControl x:Class="AvengersUtd.Explore.Environment.Templates.UGTempleteObiettivoRagg"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Width="280" Height="376"
             MinWidth="280" MinHeight="376"
             FontFamily="Tahoma"
             x:Name="container" x:FieldModifier="private">

    <StackPanel Height="376" Background="{Binding ElementName=container, Path=Image}" Orientation="Vertical" >
        <Border BorderBrush="LightGray" BorderThickness="1" Background="Transparent"></Border>
        <StackPanel Orientation="Vertical" Margin="0 40 0 0" Height="320">
            <TextBlock Text="{Binding ElementName=container, Path=Caption}" HorizontalAlignment="Left"  Height="34" FontWeight="Bold" FontSize="20" Margin="0 0 0 0" Foreground="Navy" />
            <TextBlock Text="{Binding ElementName=container, Path=Description}" Height="250" FontSize="14" TextWrapping="Wrap" />
            <TextBlock Text="Quit" Margin="32 4 0 0" Foreground="White" FontSize="14" FontWeight="Normal" />
        </StackPanel>
    </StackPanel>

</UserControl>
